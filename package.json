{
  "name": "vietincare-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "db:check": "node --env-file-if-exists=.env server/migrate.js --check",
    "db:init": "node --env-file-if-exists=.env server/migrate.js --init",
    "db:migrate": "node --env-file-if-exists=.env server/migrate.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": { "express": "^5.1.0", "mysql2": "^3.14.0" }
}
