# Checklist tiến độ VietinCare AI

Cập nhật gần nhất: 2026-09-23 (Asia/Saigon).

Đây là nơi theo dõi công việc chung của project. Mỗi lần sửa mã nguồn, SQL hoặc tài liệu, trợ lý đọc và cập nhật file này theo `AGENTS.md`, rồi nhắc người dùng việc tiếp theo trong câu trả lời cuối.

## Trạng thái hiện tại

- 2026-09-23 · AI-05: người dùng yêu cầu Gemini API Key. Đã thêm adapter Gemini, lựa chọn provider, lời nhắc khóa ẩn và nhãn VI/EN; kiểm thử MySQL/API đạt 43/43, 0 skip, với transport AI giả. Không có khóa thật và chưa gọi Google thật.

- Đã triển khai ADMIN-02 và mã/UI AI-01: kho tri thức VI/EN, phiên bản/xuất bản, OpenAI Embeddings/Responses và chat có nguồn lưu MySQL. Kiểm thử ngày 2026-09-22: 37/37 đạt, 0 skip, với MySQL thật và AI giả lập; trình duyệt desktop/mobile đạt các luồng mô tả trong nhật ký. Chưa gọi OpenAI thật hoặc áp dụng migration 004 trên database người dùng. Khóa được nhập ẩn khi khởi động theo lựa chọn người dùng.

- Ngày 2026-09-22: người dùng xác nhận đã tạo Agent và đăng nhập nhân viên thành công qua scripts/start-local.ps1. Đã đồng bộ giao diện sau đăng nhập VI/EN theo FE-07; cần khởi động lại backend đang chạy để phục vụ workspace.css/workspace.js mới. Đợt này không sửa database thật.

- Có frontend VI/EN cho trang chủ, xác thực, tài khoản và ticket. Luồng tạo ticket → Agent nhận/phản hồi → giải quyết/đóng → Customer xem kết quả đã kiểm thử API và trình duyệt trên MySQL tạm; đã bổ sung lưu lịch sử hội thoại Customer VI/EN, đã có mã/UI RAG, còn kiểm chứng OpenAI thật và chưa triển khai chuyển giao chat. Cần áp dụng migration mới nhất (004, tổng 22 bước) và khởi động lại server trên máy người dùng.
- Có script MySQL 8.0.46 tạo 15 bảng, gồm `KnowledgeChunk`; đã có `server/`, migration xác thực/hỗ trợ và test tích hợp, migration đã được xác nhận current (15 bước) và log người dùng xác nhận 10/10 test đạt trên MySQL, không skip.
- Ba Word hiện có (`02_ban2.docx`, `03_ban2.docx`, `04_ban1.docx`) đã sửa nội dung phạm vi phiên bản 1.1, chỉ dùng MySQL; sơ đồ ảnh cũ được thay bằng luồng/quan hệ chữ có thể chỉnh sửa. Chưa render kiểm tra bố cục và chưa hoàn thiện lại UML/ERD đồ họa.
- Phạm vi nghiệm thu theo `PROJECT_SCOPE.md`: web VI/EN, bốn vai trò, RAG, ticket, chuyển giao, CSAT và quản trị cơ bản. Các yêu cầu mở rộng vẫn giữ trong backlog nhưng không phải điều kiện nghiệm thu cốt lõi.
- Các kết quả kiểm tra trước đây bên dưới được ghi lại từ phiên làm việc ngày 2026-09-20, không phải vừa chạy lại khi tạo checklist.
- Các trang trong `VI/README.txt` là cấu trúc đề xuất; không coi chúng là các chức năng đã được xây dựng.

## Cách ghi tiến độ

- `[x]`: đã đạt tiêu chí của mục; `[ ]`: chưa hoàn thành. Ghi thêm **Đang làm**, **Chưa kiểm chứng** hoặc **Bị chặn** trong nội dung nếu cần.
- P0: sửa nền tảng trước; P1: luồng demo nghiệp vụ cốt lõi; P2: mở rộng và hoàn thiện nghiệm thu.
- Khi bắt đầu một mục, ghi ID vào phần “Việc tiếp theo”. Sau khi sửa, cập nhật mục và thêm nhật ký kèm kiểm tra thực tế.
- Giữ ID ổn định. Không dùng tỷ lệ hoàn thành suy đoán hoặc kết quả Pass cũ để đại diện cho phiên bản mới.

## Việc tiếp theo

**AI-01, ADMIN-02 · Bước tiếp theo:** dừng backend, chạy manage-local.ps1 -Action migrate (004, tổng 22 bước), tạo Admin nếu chưa có, chạy start-local.ps1 -EnableAI -AIProvider gemini và nhập khóa Gemini trên máy (lựa chọn mới nhất). Theo RAG_GUIDE.md: nhập FAQ → tạo dữ liệu tìm kiếm → thử → xuất bản → Customer hỏi và kiểm tra nguồn. Sau đó đánh giá câu trả lời/thiếu nguồn bằng API thật; HAND-01 là ưu tiên nghiệp vụ tiếp theo. 37/37 kiểm thử bằng provider giả không xác nhận chất lượng AI thật.

**CHAT-01 · Đã triển khai phần lưu lịch sử ngày 2026-09-22:** Customer lưu/mở lại tin nhắn, phân trang, kết thúc phiên và gửi lại không trùng; VI/EN đồng bộ workspace. 26/26 test MySQL/API đạt, 0 skip; đã kiểm tra trình duyệt desktop/mobile, lỗi kết nối và tạo phiên mới sau kết thúc. Cần migrate 003 (thêm 3 bước sau version 2, tổng 19), restart backend và kiểm tra trên localhost:3000. AI-01/RAG đã nối trong đợt tiếp theo, xem ghi nhận mới nhất; chưa nghiệm thu toàn bộ UC002 với API thật. Hướng dẫn: CHAT_GUIDE.md.

**FE-07 đã hoàn thành ngày 2026-09-22:** tổng quan theo vai trò, ticket và điều hướng chung VI/EN; kiểm thử 19/19 trên MySQL tạm, kiểm tra trình duyệt desktop/mobile. Ưu tiên hiện tại: khởi động lại backend và kiểm tra giao diện mới trên localhost:3000; sau đó triển khai CHAT-01/RAG theo phạm vi được giao. Ghi nhận mới này thay thế phần chờ xác nhận tạo Agent/đăng nhập trong các ghi chép trước; chưa có xác nhận người dùng về luồng ticket trên database thật.

**TK-01 đến TK-03 đã triển khai/kiểm thử ngày 2026-09-22 trên MySQL tạm cổng 33316.** Cần chạy scripts/manage-local.ps1 -Action migrate trên máy người dùng (thêm 1 bước, tổng 16), tạo Agent bằng -Action create-agent, rồi khởi động scripts/start-local.ps1. Hướng dẫn: TICKET_GUIDE.md. Chưa sửa database người dùng cổng 3306 trong đợt này.

**DOC-02, DOC-04:** nội dung Word đã sửa ngày 2026-09-21; cần render/kiểm tra bố cục và hoàn thiện UML/ERD. **DB-02 đến DB-06** đã có một phần mã migration trong `migrations/001_auth_and_support.sql`; cần đối chiếu đủ thiết kế đích và kiểm thử áp dụng an toàn trên MySQL. Phiên đồng bộ tài liệu chưa thay đổi SQL hoặc database.

**DB-07 hoàn thành khởi tạo cơ bản:** log người dùng ngày 2026-09-21 xác nhận MySQL 8.0.46 và đủ 15 bảng trong `qlkhviettin`, gồm `knowledgechunk`. Log mới của người dùng xác nhận db:check trả current, steps: 15: nhật ký migration ghi đủ 15 bước hoàn tất và checksum phù hợp. Log mới xác nhận 10/10 test đạt, gồm 9 test tích hợp MySQL/API. Người dùng đã cung cấp nội dung trang tài khoản tiếng Việt xác thực thành công vai trò Customer; người dùng xác nhận F5 giữ phiên, đăng xuất về đăng nhập và mở lại trang tài khoản bị chuyển về đăng nhập. Còn kiểm tra EN và bố cục desktop/mobile.

1. **DB-08, BE-01, RELEASE-01**: chạy công cụ thiết lập tài khoản MySQL ứng dụng riêng, xác nhận khởi động bằng tài khoản này; script và hướng dẫn đã chuẩn bị.
2. **FE-04, DB-02, DB-03**: kiểm tra giao diện EN/mobile và hoàn thiện kiểm thử quyền sở hữu nghiệp vụ; xác thực backend hiện có đã đạt 10/10 test.
3. **TK-01 đến TK-03, RELEASE-01**: áp dụng migration 002 và xác nhận luồng ticket trên máy người dùng theo TICKET_GUIDE.md; mã và kiểm thử trên MySQL tạm đã hoàn thành.

## Những việc đã hoàn thành

- [x] **FE-01 · P0** Sửa phạm vi biến trong chat, gửi trùng từ ô preview và dùng `textContent` cho tin nhắn. File: `script.js`. Kiểm tra trước đây: chat có phản hồi, gửi một lần, bỏ qua tin trống và hiển thị đầu vào dưới dạng văn bản.
- [x] **FE-02 · P0** Khôi phục form đăng ký tiếng Việt; sửa đường dẫn CSS/JS, chuyển ngôn ngữ và điều hướng VI/EN. File: `VI/`, `EN/`, `script.js`. Kiểm tra trước đây: tổng cộng 72 kiểm tra bằng Node cho liên kết, ID, form và hành vi JavaScript; chưa thay thế kiểm thử trình duyệt.
- [x] **FE-03 · P0** Nêu rõ trạng thái demo trong thông báo đăng nhập/đăng ký; thêm Enter ở ô chat preview; tách CSS trang chủ VI vào `landing-vi.css`.
- [x] **DB-01 · P0** Chuyển `SQLQuery1.sql` sang MySQL 8.0.46, InnoDB, utf8mb4, `AUTO_INCREMENT`, `DATETIME(3)` và `LONGTEXT`; bổ sung `KnowledgeChunk` cho RAG. Đã tạo thành công 15 bảng trên MySQL tạm; kiểm tra tiếng Việt/emoji, JSON, liên kết ticket và engine/collation đạt. Script kiểm tra: `.mysql-sync/smoke.sql`. Database tạm đã được dọn dẹp.
- [x] **DOC-01 · P0** Cập nhật nội dung MySQL/RAG trong `03_ban2.docx`, `04_ban1.docx`, `05_GenAI_SoftwareDevelopment_functional-testing.docx`; thêm hướng dẫn trong `README.md`. Kiểm tra XML đạt, không còn tên hệ quản trị cũ trong phần văn bản được quét. Báo cáo ghi rõ cần chạy lại kiểm thử nghiệp vụ trên MySQL. Kiểm tra bố cục vẫn thuộc DOC-02.
- [x] **TRACK-01 · P0** Tạo checklist, quy tắc tự cập nhật trong `AGENTS.md` và liên kết từ README. Hoàn thành khi file có trạng thái thực tế, công việc tiếp theo và nhật ký.

## Nền tảng dữ liệu và backend

- [ ] **DB-08 · P0 · Chờ chạy thực tế** Tài khoản MySQL ứng dụng riêng. Đã thêm scripts/setup-db-user.ps1, scripts/setup-db-user.js và scripts/start-local.ps1, hướng dẫn VI/EN trong README. Công cụ tạo vietincare_app tại 127.0.0.1 với SELECT/INSERT/UPDATE/DELETE trên qlkhviettin; hỏi mật khẩu ẩn, không ghi bí mật vào project; dừng nếu tên tài khoản đã tồn tại. Đạt khi thiết lập, kiểm tra quyền và khởi động/đăng nhập ứng dụng bằng tài khoản riêng thành công. Chưa có thông tin xác thực trong phiên trợ lý nên chưa tạo tài khoản hoặc kiểm tra MySQL thực tế.

- [x] **DB-07 · P0** Đã khởi tạo database project trên MySQL 8.0.46 máy người dùng. Bằng chứng: trước khởi tạo SHOW TABLES trả Empty set; sau nạp SQLQuery1.sql, log người dùng ngày 2026-09-21 có đủ 15 bảng đúng danh mục, gồm knowledgechunk. Hoàn thành phạm vi khởi tạo cơ bản; log db:check tiếp theo xác nhận migration current, steps: 15. Chưa kiểm chứng nghiệp vụ/backend hoạt động.
- [ ] **DB-02 · P0** Liên kết `Customer` với `UserAccount`; chốt tài khoản khách hàng và chat khách vãng lai. Đạt khi từ tài khoản đăng nhập xác định được đúng khách hàng, không trùng thông tin định danh ngoài chủ đích.
- [ ] **DB-03 · P0** Ràng buộc quyền sở hữu ticket/phiên chat/đánh giá; quy định mục tiêu của mỗi đánh giá. Đạt khi dữ liệu khác khách hàng bị từ chối và có kiểm thử trường hợp sai.
- [ ] **DB-04 · P0** Chuẩn hóa `NULL/NOT NULL`, trạng thái, mức ưu tiên, thang tin cậy AI, cảm xúc, điểm NPS và thứ tự ngày giờ. Đạt khi SQL, tài liệu và dữ liệu kiểm thử cùng quy tắc.
- [ ] **DB-05 · P1** Bổ sung định danh người gửi, phản hồi ticket độc lập với phiên chat, lịch sử trạng thái và mốc chuyển giao. Đạt khi truy vết được ai xử lý/gửi tin và khi nào.
- [ ] **DB-06 · P1** Tạo migration, dữ liệu mẫu không nhạy cảm và index theo truy vấn thực tế. Đạt khi nâng cấp được database có dữ liệu mà không xóa dữ liệu cũ.
- [ ] **BE-01 · P0 · Đang làm** Đã chọn Node.js/Express, có cấu trúc API và cấu hình MySQL qua biến môi trường. Log người dùng ngày 2026-09-21 xác nhận npm.cmd run db:check trả ready, remaining: 15, chứng minh công cụ Node kết nối MySQL và kiểm tra trước migration đạt; log test mới xác nhận API HTTP trên database thử đã hoạt động (10/10 test đạt); người dùng đã cung cấp nội dung trang tài khoản VI xác thực thành công, xác nhận backend phục vụ giao diện tài khoản. Còn cấu hình tài khoản MySQL riêng cho ứng dụng và hoàn thiện hướng dẫn chạy.
- [x] **AUTH-01 · P1 · UC001/UC012 · Đạt kiểm thử backend** Log người dùng ngày 2026-09-21: RUN_MYSQL_TESTS=1, npm.cmd test đạt 10/10, 0 fail, 0 skip (khoảng 6,04 giây). Bao gồm đăng ký, trùng email, mật khẩu sai, session HttpOnly, bốn vai trò, từ chối dữ liệu khách khác, giới hạn thử, tài khoản bị khóa, hết phiên và đăng xuất. File: server/app.js, server/password.js, tests/integration.test.js, tests/password.test.js. Người dùng đã xác nhận trang tài khoản VI hiển thị xác thực thành công; người dùng xác nhận F5 giữ phiên, đăng xuất và chặn truy cập trang tài khoản sau đăng xuất đúng; kiểm thử trình duyệt đầy đủ tiếp tục theo FE-04; không suy ra các API nghiệp vụ khác đã hoàn thành.
- [ ] **AUTH-02 · P2 · UC001 · Mở rộng** OTP và quên mật khẩu qua email. Giới hạn thử đăng nhập và hết hạn phiên thuộc AUTH-01 cốt lõi. Đạt khi kiểm tra cả hết hạn, mã sai và gửi lại.

## Khách hàng và nhân viên hỗ trợ

- [x] **TK-01 · P1 · UC003** Đã triển khai tạo ticket trả mã TK-xxxxxx, lưu MySQL, kiểm tra đầu vào và chống gửi trùng qua TicketRequest. File: server/tickets.js, migrations/002_ticket_requests.sql, tickets.js, VI/tickets-vi.html, EN/tickets-en.html. API và thao tác tạo ticket VI trong trình duyệt đạt trên MySQL tạm ngày 2026-09-22; chờ áp dụng migration 002 trên database người dùng.
- [x] **TK-02 · P1 · UC007** Đã triển khai danh sách/chi tiết/phản hồi/lịch sử theo khách hàng, lọc trạng thái và phân trang 30 mục. API từ chối khách khác; trình duyệt xác nhận còn dữ liệu sau F5 và khách xem được phản hồi/trạng thái đóng. Cùng file TK-01 và tests/tickets.integration.test.js; kiểm thử MySQL tạm, chờ nâng cấp môi trường người dùng.
- [x] **TK-03 · P1 · UC007** Agent nhận ticket, phản hồi, sửa ưu tiên, giải quyết rồi đóng; khóa hàng tránh nhận đồng thời, chỉ Agent sở hữu được xử lý, có lịch sử và audit. Supervisor chỉ đọc; phân công lại chưa triển khai. API và trình duyệt đạt trên MySQL tạm ngày 2026-09-22. Có server/provision-agent.js và scripts/manage-local.ps1 tạo Agent bằng quản trị cục bộ, không nâng quyền qua đăng ký công khai. Chờ áp dụng migration và tạo Agent trên máy người dùng.
- [ ] **CHAT-01 · P1 · UC002 · Đạt phần lưu lịch sử** Đã có API/giao diện Customer VI/EN: tạo phiên bằng tin đầu, lưu/tải lịch sử, phân trang 30 phiên/50 tin, kết thúc phiên chỉ xem, chống gửi trùng qua ChatRequest và giữ ô nhập khi lỗi mạng. Migration 003 giữ phiên/tin cũ, tổng 19 bước. File: server/chats.js, chat.js, chat.css, VI/chat-vi.html, EN/chat-en.html, workspace.js, account.js, server/app.js, server/index.js, server/migrate.js, tests/chats.integration.test.js, tests/tickets.integration.test.js và CHAT_GUIDE.md. Kiểm thử 26/26 MySQL/API đạt và trình duyệt VI/EN desktop/mobile đạt phần lưu lịch sử. Đợt RAG bổ sung trạng thái chờ AI, thử lại và nguồn tham khảo; kiểm thử luồng bằng provider giả đạt. Người dùng xác nhận đã hoàn thành bước trước; chưa có log xác nhận migration 004 (tổng 22) hoặc OpenAI thật. Không coi toàn bộ UC002 hoàn thành.
- [ ] **HAND-01 · P1 · UC006** Chuyển AI sang nhân viên kèm lịch sử, hàng chờ, tiếp nhận và trường hợp không có nhân viên trực.
- [ ] **EVAL-01 · P1 · UC010** CSAT cốt lõi: chủ sở hữu đánh giá một lần, 1–5 sao cho đúng một phiên kết thúc hoặc ticket đóng. NPS thuộc mở rộng. Quy tắc và công thức tại `PROJECT_SCOPE.md`; chưa triển khai.

## AI và cơ sở tri thức

- [ ] **AI-05 · P1 · Gemini · Chờ kiểm chứng** Tích hợp Google Gemini cho embedding và trả lời có nguồn; giữ OpenAI tùy chọn. Kiểm thử transport và MySQL đạt 43/43 ngày 2026-09-23; chưa có API key thật, chưa nghiệm thu chất lượng Gemini. Không dùng lẫn vector giữa các model.

- [ ] **AI-01 · P1 · UC002/UC011 · Chờ kiểm chứng API thật** Đã triển khai nhập văn bản, chia đoạn/embedding lưu MySQL, truy xuất tài liệu xuất bản, câu trả lời kèm bản chụp nguồn, thiếu nguồn/lỗi/thử lại và khóa chỉ ở backend. 37/37 test đạt với AI giả lập; chưa xác nhận khóa/quota/model thật, chất lượng truy xuất và ngưỡng cosine 0,35. File và bằng chứng trong nhật ký 2026-09-23; không coi AI thật đã đạt.
- [ ] **AI-02 · P1 · UC004/UC005** Phân loại ticket, mức ưu tiên và cảm xúc; định nghĩa cách tính ngưỡng tin cậy. Đạt khi có bộ câu hỏi gán nhãn để đánh giá, gồm tình huống phàn nàn bị phân loại sai trước đây.
- [ ] **AI-03 · P2 · UC008 · Mở rộng** AI Copilot gợi ý phản hồi cho Agent; nhân viên được sửa và chủ động gửi, có ghi nhận gợi ý được chọn.
- [ ] **AI-04 · P2 · UC009 · Mở rộng** Tóm tắt khi chuyển giao/kết thúc hội thoại, lưu kèm nguồn hội thoại và hiển thị cho nhân viên.

## Quản trị và giao diện

- [ ] **ADMIN-01 · P1 · UC012** Quản lý tài khoản, nhân viên và vai trò; kiểm tra quyền ở backend; thuộc cốt lõi.
- [x] **ADMIN-02 · P1 · UC011** Đã triển khai cốt lõi FAQ/văn bản: bản nháp, kiểm soát phiên bản, tạo dữ liệu tìm kiếm, thử câu hỏi, xuất bản/thay thế/ngừng sử dụng; chỉ Admin. API MySQL và giao diện VI/EN desktop/mobile đã kiểm tra ngày 2026-09-22 bằng AI giả lập. Chất lượng OpenAI thật còn tại AI-01. Tải PDF/DOCX thuộc mở rộng, chưa triển khai.
- [ ] **ADMIN-03 · P1 · UC013** Dashboard cốt lõi: ticket theo trạng thái/chủ đề, số phiên, CSAT thực từ MySQL khi làm mới. Chỉ số SLA, hiệu quả AI nâng cao và xuất file thuộc mở rộng.
- [ ] **ADMIN-04 · P1 · UC014/UC015** Audit log và cấu hình không chứa bí mật thuộc cốt lõi. Đã sửa tên UC015 thành Cấu hình hệ thống trong tài liệu; chức năng và schema cấu hình chưa triển khai.
- [ ] **FE-04 · P1 · Đạt một phần** Ngày 2026-09-21 người dùng gửi nội dung trang tài khoản VI xác thực thành công và vai trò Khách hàng. Người dùng đã xác nhận tải lại giữ phiên, đăng xuất về đăng nhập, mở lại trang tài khoản sau đăng xuất bị chuyển về đăng nhập. Ngày 2026-09-22 đã kiểm tra trang ticket VI/EN bằng Codex in-app browser: desktop ảnh 1265×714 và viewport mobile 390×844, tạo/nhận/phản hồi/giải quyết/đóng, đọc lại phía khách, tải lại và hiển thị HTML như văn bản. Chưa kiểm tra toàn bộ trang/menu/chat hoặc thiết bị mobile thật. Kiểm tra thực tế trên trình duyệt desktop/mobile: chat, đăng ký, đăng nhập, menu, đổi kích thước và chuyển ngôn ngữ; ghi trình duyệt/kích thước và kết quả.
- [ ] **FE-05 · P2** Hoàn thiện các liên kết placeholder, FAQ/liên hệ, nhãn hỗ trợ truy cập và nội dung VI/EN. Chỉ công bố các khả năng/số liệu đã có hoặc đánh dấu demo rõ ràng.
- [ ] **FE-06 · P2** Rà soát CSS chung và CSS dashboard để tránh trùng `.logo`, `.nav`, `.hero`; xác nhận không làm thay đổi giao diện ngoài dự kiến.

## Tài liệu, kiểm thử và nghiệm thu

- [x] **DOC-05 · P1** Đã đối chiếu danh mục ngày 2026-09-21 và sửa README theo 3 file thực tế 02/03/04. File 05 chưa tìm thấy; ghi rõ thiếu thay vì coi đã có. Không tạo hoặc khôi phục kết quả kiểm thử cũ; hồ sơ kiểm thử mới thuộc QA-01/QA-02.
- [x] **SCOPE-01 · P0** Theo yêu cầu người dùng ngày 2026-09-21, đã đồng bộ phạm vi cốt lõi/mở rộng, MySQL duy nhất và tiêu chí nghiệm thu trong 3 Word, `PROJECT_SCOPE.md`, README và checklist. Mốc 27/09/2026 giữ theo kế hoạch gốc, không dùng để khẳng định tiến độ. Kiểm tra nội dung XML; bố cục thuộc DOC-02, sơ đồ đồ họa thuộc DOC-04. Không đánh dấu chức năng triển khai hoàn thành.
- [ ] **DOC-02 · P1 · Chưa kiểm chứng** Render và xem bố cục cả 3 Word sau chỉnh sửa; kiểm tra cả sơ đồ dạng ảnh. Trước đây chưa làm được vì thiếu bộ render; kiểm tra XML không xác nhận hình ảnh hoặc phân trang.
- [ ] **DOC-03 · P1 · Chờ kiểm tra bố cục** Đã sửa nội dung 02/03/04: chat trước/ticket khi cần, bốn trạng thái ticket, ba cảm xúc, đăng nhập Customer, vai trò Supervisor, UC015 cấu hình; p95 ≤ 5 giây trên 50 câu hỏi/5 phiên/1.000 đoạn. Đã thay ảnh sơ đồ cũ bằng luồng chữ đồng bộ. Chưa render để xác nhận tài liệu hoàn chỉnh.
- [ ] **DOC-04 · P2** Đồng bộ ERD/mô hình với SQL triển khai, gồm `KnowledgeChunk`; sửa các lỗi đánh số, thuộc tính lặp và ngày TC04 `16/2026` sau khi xác minh ngày đúng.
- [ ] **QA-01 · P1** Lập ma trận UC → API/trang → test case → bằng chứng; mở rộng 5 test case hiện có cho lỗi đầu vào, quyền, trạng thái và ngoại lệ.
- [ ] **QA-02 · P1 · Đạt một phần** Log người dùng ngày 2026-09-21 xác nhận 10/10 test hiện có đạt (9 tích hợp, 1 mật khẩu), 0 skip. Ngày 2026-09-22 trợ lý chạy bản mới đạt 19/19 test, 0 skip trên MySQL tạm 8.0.46 và kiểm tra trình duyệt ticket VI/EN. Còn các nghiệp vụ chưa triển khai và môi trường người dùng sau nâng cấp. Chạy lại kiểm thử tích hợp MySQL/backend/frontend; chỉ ghi Pass khi có kết quả thực tế theo phiên bản. Smoke test tạo bảng không thay thế kiểm thử nghiệp vụ.
- [ ] **QA-03 · P1** Kiểm thử toàn luồng: khách hàng chat → tạo ticket → nhân viên nhận/phản hồi → hoàn thành → khách hàng đánh giá.
- [ ] **QA-04 · P2** Đo hiệu năng AI theo mục tiêu đã thống nhất; kiểm tra hết phiên, lỗi AI, tải lên, dữ liệu nhạy cảm và quyền truy cập. Không suy ra các cam kết bảo mật/uptime từ một lần demo.
- [ ] **RELEASE-01 · P2** Hoàn thiện hướng dẫn cài đặt/chạy, cấu hình mẫu, dữ liệu demo, backup/khôi phục và kịch bản trình bày; người khác có thể dựng môi trường theo README.

- [x] **FE-07 · P1** Đồng bộ giao diện sau đăng nhập VI/EN: sidebar/topbar/menu mobile dùng chung, tổng quan theo vai trò, 5 ticket mới nhất từ API, hồ sơ, liên kết hàng chờ/lọc trạng thái, khung danh sách/trao đổi/lịch sử. File: workspace.js, workspace.css, account.js, account.css, tickets.js, tickets.css, VI/EN account/tickets HTML, server/app.js (chỉ thêm allowlist tài nguyên). Kiểm tra ngày 2026-09-22: cú pháp JS đạt; 19/19 test MySQL/API đạt, 0 skip; trình duyệt desktop 1265×714 và viewport 390×844, bốn vai trò, VI/EN, tạo/nhận/phản hồi và menu đóng/mở đạt. Chưa kiểm tra điện thoại thật hoặc bản mới trên backend đang chạy cổng 3000. Không triển khai AI/báo cáo/quản lý người dùng.

## Nhật ký thay đổi

Ngày 2026-09-22 · CHAT-01, FE-04, DB-03, DB-06, RELEASE-01: Bổ sung server/chats.js với xác thực Customer, kiểm tra chủ sở hữu, khóa tài khoản/phiên trong transaction, journal ChatRequest gắn người gửi để retry đồng thời không trùng. Tạo phiên và tin đầu cùng transaction; API list/detail/messages/close, phân trang, chặn gửi sau kết thúc, ghi audit không chứa nội dung chat. Migration 003 thêm 3 bước giữ dữ liệu cũ; không sửa checksum 001/002. Nối app/static allowlist, kiểm tra ChatRequest lúc startup. Thêm chat.js/chat.css và VI/EN chat pages; nối sidebar và shortcut Customer trên account.js, không cấp quyền chat riêng của khách cho nhân viên. UI dùng textContent, hiển thị rõ AI chưa nối, lưu thành công khác với tải lại thất bại, timeout 20 giây và retry key/hash trong sessionStorage không lưu nội dung. Tài liệu: CHAT_GUIDE.md, README.md, migrations/README.md, PROJECT_SCOPE.md.

Kiểm tra CHAT-01 ngày 2026-09-22: node --check đạt chat.js/workspace.js/server/chats.js/tests/chats.integration.test.js. Chạy RUN_MYSQL_TESTS=1 npm test trên MySQL Community 8.0.46 tạm 33316: lần đầu lỗi assertion COUNT trả chuỗi, sửa so sánh Number; lần cuối 26 pass, 0 fail, 0 skip, 9,97 giây. Gồm nâng v2 lên v3 giữ tin tiếng Việt/emoji, chạy lại migration, đồng thời gửi/retry, xung đột key, sai chủ sở hữu/vai trò/origin/đầu vào, retry sau tạo lại app, phân trang, gửi đồng thời kết thúc và readonly. Runtime test chỉ có CRUD. CUA browser desktop 1265×714 và viewport 390×844: Customer tạo tin VI, hiển thị thẻ HTML như văn bản, F5 giữ lịch sử, EN giữ đúng phiên, dừng Node preview mô phỏng mất kết nối giữ ô nhập, khởi động lại rồi retry chỉ lưu một tin, kết thúc và F5 vẫn readonly. Lần kiểm tra cuối bị review hết hạn mức; sau khi người dùng yêu cầu tiếp tục đã kiểm tra được New → lưu CHAT-2 riêng trong khi CHAT-1 vẫn Ended. Không còn chặn do hạn mức tại lúc bàn giao. Chưa kiểm tra điện thoại thật, toàn bộ trình duyệt hoặc database người dùng; AI/RAG chưa nối, CHAT-01 giữ trạng thái đạt một phần.


Ngày 2026-09-22 · FE-07, FE-04, TK-03: Người dùng xác nhận đăng nhập Agent thành công; không lưu thông tin tài khoản cá nhân. Thay trang tài khoản đơn giản bằng tổng quan có lời chào, thao tác theo vai trò, hồ sơ và 5 yêu cầu mới tạo lấy từ API. Shared shell có menu responsive, avatar, đổi ngôn ngữ, đăng xuất; Admin ẩn ticket, Supervisor chỉ đọc. Ticket giữ nguyên API/nghiệp vụ, thêm bộ lọc từ liên kết tổng quan, màu trạng thái, thẻ phản hồi và dòng thời gian. File được nêu tại FE-07. Kiểm tra cú pháp workspace.js/account.js/tickets.js/server/app.js đạt. RUN_MYSQL_TESTS=1 npm test trên MySQL 8.0.46 tạm cổng 33316: 19 pass, 0 fail, 0 skip, 5,60 giây. Dùng Codex in-app browser với tài khoản giả trong database preview: tổng quan Customer/Agent/Supervisor/Admin hiển thị đúng; VI/EN và desktop/mobile đã xem ảnh; tạo ticket VI, danh sách tổng quan hiển thị ticket thật, Agent nhận/phản hồi EN, Supervisor xem lịch sử không có nút sửa, Admin không có link ticket; đăng xuất từ ticket về login; kiểm tra link hàng chờ chọn đúng trạng thái Mới. Sửa vùng backdrop đóng menu và xác nhận AX trả collapsed, nội dung có thể thao tác. Chưa chạy lại toàn bộ lifecycle đóng ticket bằng trình duyệt trong đợt UI này (đã có kiểm thử API 19/19 và bằng chứng trình duyệt đợt trước). Không sửa migration/SQL/DB thật, không chạy toàn bộ backlog.
Ngày 2026-09-22 · TK-03, AUTH-01, DB-08, BE-01: Log người dùng xác nhận create-agent trả AGENT_CREATED; tài khoản website Agent đã được tạo. Log start-local.ps1 hiện VietinCare: http://localhost:3000, xác nhận server từng khởi động qua script đặt DB_USER=vietincare_app. Sau đó PowerShell trở lại dấu nhắc và người dùng bắt đầu create-agent lần nữa. Trợ lý GET /api/health lúc kiểm tra bị connection refused: hiện không có backend phản hồi tại localhost:3000. Hướng dẫn Ctrl+C hủy lần tạo lặp, khởi động lại start-local.ps1 và giữ cửa sổ mở, đăng nhập bằng email/mật khẩu website Agent vừa tạo. Không lưu email/mật khẩu vào checklist, không tạo lại hoặc reset tài khoản. Chưa có bằng chứng đăng nhập Agent thành công trên máy người dùng; DB-08/TK-03 xác minh môi trường thực còn chờ. Chỉ cập nhật PROJECT_CHECKLIST.md. Tiếp theo: khởi động server, xác nhận vai trò Agent, thử nhận/phản hồi ticket.
Ngày 2026-09-22 · AUTH-01, TK-03, BE-01: Người dùng xác nhận lỗi INVALID_CREDENTIALS xảy ra khi đăng nhập vai trò nhân viên trên môi trường đang dùng. Đọc server/provision-agent.js, scripts/manage-local.ps1 và điều kiện đăng nhập server/app.js: công cụ chỉ báo AGENT_CREATED sau transaction tạo Agent thành công; đăng nhập yêu cầu email/mật khẩu website đúng và tài khoản hoạt động. Chưa có log create-agent từ máy người dùng nên chưa xác định tài khoản đã tạo, trùng email, lỗi đầu vào hoặc sai thông tin đăng nhập; không tự reset mật khẩu/đổi vai trò. Yêu cầu phần kết quả cuối của lệnh create-agent, không yêu cầu mật khẩu. Chỉ cập nhật checklist; kết quả kiểm thử trên MySQL tạm giữ nguyên, xác nhận TK-03 trên môi trường người dùng vẫn chưa đạt.
Ngày 2026-09-22 · TK-01, TK-02, TK-03, DB-03, DB-05, DB-06, FE-04, QA-02, RELEASE-01: Hoàn thành đợt triển khai ticket theo yêu cầu làm tiếp. Thêm server/tickets.js, server/http-error.js; nối API và static allowlist vào server/app.js; server/index.js kiểm tra TicketRequest khi khởi động. Thêm migration 002_ticket_requests.sql, mở rộng server/migrate.js chạy nhiều version có checksum/journal, giữ nguyên migration 001 đã áp dụng. Thêm tickets.js/tickets.css và VI/EN tickets pages, liên kết từ account pages/account.js. Chức năng: Customer tạo/xem/phản hồi đúng chủ sở hữu, mã TK-xxxxxx, lọc/phân trang, Agent nhận/phản hồi/ưu tiên/giải quyết/đóng, Supervisor chỉ đọc, lịch sử/audit và chống gửi trùng MySQL. Thêm server/provision-agent.js, scripts/manage-local.ps1, TICKET_GUIDE.md, migrations/README.md; cập nhật README/PROJECT_SCOPE.md. Không triển khai RAG/CSAT/handover/reassignment hoặc toàn bộ backlog.

Kiểm tra thực tế đợt 2026-09-22: MySQL Community 8.0.46 tạm riêng tại .mysql-test/tickets-20260921, chỉ bind 127.0.0.1:33316. RUN_MYSQL_TESTS=1 node --test tests/*.test.js: 19 test, pass 19, fail 0, skip 0 (~9,42 giây); gồm migration v1→v2 giữ ticket cũ, provisioning Agent/trùng email, runtime không có CREATE TABLE, đồng thời tạo trùng/nhận ticket, ownership/roles, validate, phản hồi/lịch sử/trạng thái/đóng và phân trang. Test runtime dùng quyền SELECT/INSERT/UPDATE/DELETE, không quyền DDL. JS syntax check và parser scripts/manage-local.ps1 đạt. Codex in-app browser trên server thử localhost:3001: Customer VI tạo TK-000001, F5 giữ dữ liệu, HTML thử hiển thị nguyên văn; Agent nhận/phản hồi/giải quyết/đóng; Customer EN xem đúng phản hồi, trạng thái và bốn mốc lịch sử; xem ảnh desktop 1265×714, mobile viewport 390×844 VI/EN không thấy tràn ngang ở vùng đã kiểm tra. Dữ liệu UI chỉ là tài khoản/example.test và nội dung giả lập. Viewport được khôi phục. Các bảng/database test tích hợp tự dọn; server/database preview được dừng và dọn riêng cuối phiên.

Giới hạn bàn giao 2026-09-22: chưa chạy migration 002, tạo Agent hoặc restart server của người dùng cổng 3306/3000 vì thông tin xác thực chỉ nằm trong phiên người dùng; không lưu mật khẩu thật vào project. TK-01 đến TK-03 đánh dấu đạt triển khai/kiểm thử, không có nghĩa đã nâng cấp database thật. DB-08 vẫn cần bằng chứng chạy app user; FE-04/QA-02/QA-03/RELEASE-01 vẫn mở cho phạm vi còn lại. Tiếp theo người dùng chạy manage-local.ps1 -Action migrate, create-agent, rồi start-local.ps1 và demo theo TICKET_GUIDE.md.
Ngày 2026-09-21 · FE-04, AUTH-01, BE-01, DB-08: Người dùng xác nhận đã hoàn thành sau hướng dẫn mở chính xác http://localhost:3000/VI/login-vi.html và tải lại. Ghi nhận lỗi truy cập sai cổng/host đã được xử lý theo xác nhận người dùng, đăng nhập tại địa chỉ đúng hoạt động. Không suy ra tài khoản MySQL đang chạy là vietincare_app vì chưa có log APP_USER_READY hoặc bằng chứng định danh kết nối. DB-08 tiếp tục chờ xác nhận tài khoản/quyền; FE-04 còn EN/mobile và kiểm tra giao diện đầy đủ. Chỉ cập nhật PROJECT_CHECKLIST.md, không sửa mã/SQL và không chạy lại test. Tiếp theo: xác nhận DB-08, kiểm tra giao diện EN/mobile, triển khai luồng ticket khi được yêu cầu.
Ngày 2026-09-21 · FE-04, BE-01: Người dùng báo ORIGIN_REJECTED trên form. Kiểm tra trực tiếp backend đang chạy bằng POST /api/auth/login với body rỗng và header yêu cầu hợp lệ: Origin http://localhost:3000 trả 400/INVALID_INPUT (qua kiểm tra origin); http://127.0.0.1:3000, http://127.0.0.1:5500 và http://localhost:5500 đều trả 403/ORIGIN_REJECTED. Không gửi thông tin đăng nhập, không tác động bộ đếm hay dữ liệu vì bị từ chối trước bước xác thực. Xác nhận backend hiện chấp nhận localhost:3000; chưa biết URL hiện tại sau lần người dùng đổi trang, nên chưa khẳng định người dùng đang ở 127.0.0.1. Hướng dẫn mở chính xác localhost:3000 và tải lại; nếu còn lỗi cần URL hiện tại. Chỉ cập nhật PROJECT_CHECKLIST.md, không nới lỏng kiểm tra origin và không sửa mã. Tiếp theo xác nhận đăng nhập đúng host, kiểm tra DB-08.
Ngày 2026-09-21 · FE-04, BE-01, DB-08: Người dùng xác nhận URL đang mở là http://127.0.0.1:5500/VI/login-vi.html. Đối chiếu auth.js dùng fetch đường dẫn tương đối /api/auth/login: yêu cầu đi về cổng 5500 thay vì backend 3000 đã kiểm tra hoạt động. Xác định sai địa chỉ phục vụ frontend là nguyên nhân của luồng lỗi được báo; hướng dẫn mở http://localhost:3000/VI/login-vi.html, đúng APP_ORIGIN mặc định. Chưa nhận kết quả đăng nhập sau đổi URL, chưa xác nhận server dùng tài khoản MySQL riêng. Không sửa mã/SQL. Tiếp theo xác nhận đăng nhập đúng URL và hoàn tất DB-08.
Ngày 2026-09-21 · DB-08, BE-01, FE-04, AUTH-01: Người dùng báo trang đăng nhập hiện lỗi không kết nối dịch vụ sau hướng dẫn tài khoản MySQL riêng. Kiểm tra trực tiếp trong phiên trợ lý: MySQL80 Running; GET http://localhost:3000/api/health trả 200/status ok; GET /api/auth/me không cookie trả 401/UNAUTHENTICATED; POST /api/auth/login với tài khoản thử không tồn tại thuộc example.test và origin/header đúng trả 401/INVALID_CREDENTIALS, không phải lỗi dịch vụ. Lần thử tạo bộ đếm giới hạn đăng nhập cho IP/email thử theo cơ chế hiện có; không tạo tài khoản hoặc sửa dữ liệu người dùng. Chưa biết URL người dùng đang mở, log khi lỗi hoặc tài khoản MySQL của server hiện chạy; chưa xác định nguyên nhân, chưa xác nhận DB-08 hoàn thành. Yêu cầu URL và mã lỗi PowerShell, hướng dẫn mở đúng trang localhost:3000. Không thay đổi mã/SQL; chỉ ghi phát hiện vào PROJECT_CHECKLIST.md. AUTH-01 giữ bằng chứng test trước, chưa có bằng chứng lỗi hồi quy backend; FE-04/DB-08 vẫn mở. Tiếp theo: đối chiếu URL/log, khôi phục đăng nhập trên trang đang dùng và xác nhận tài khoản MySQL ứng dụng.
Ngày 2026-09-21 · DB-08, BE-01, RELEASE-01: Theo yêu cầu tiếp tục, thêm scripts/setup-db-user.js (tạo tài khoản local sau kiểm tra phiên bản/schema/tên trùng, cấp quyền dữ liệu giới hạn database, kiểm tra kết nối bằng tài khoản mới), scripts/setup-db-user.ps1 (nhập root/app password ẩn và xác nhận, xóa biến bí mật sau chạy), scripts/start-local.ps1 (khởi động bằng app user, ghi đè cấu hình root cũ trong tiến trình, không lưu mật khẩu). Cập nhật README hướng dẫn VI/EN, yêu cầu Node 24+, phân biệt quản trị/migration/test và tài khoản chạy ứng dụng, sửa trạng thái backend/migration đã lỗi thời. Kiểm tra node --check đạt; cả hai PowerShell được parser xác nhận không lỗi cú pháp; chạy setup-db-user.js không có thông tin xác thực trả SETUP_INPUT_REQUIRED và exit 1 đúng dự kiến, chưa kết nối hoặc sửa MySQL. Chưa chạy thiết lập tài khoản hoặc kiểm thử server với quyền mới vì mật khẩu chỉ nằm trong phiên PowerShell người dùng; không đánh dấu DB-08/BE-01/RELEASE-01 hoàn thành. Không dùng kết quả 10 test trước thay thế kiểm tra thay đổi mới. Tiếp theo người dùng chạy setup-db-user.ps1, start-local.ps1, kiểm tra đăng nhập/đăng xuất bằng tài khoản MySQL ứng dụng.
Ngày 2026-09-21 · AUTH-01, FE-04, BE-01: Người dùng xác nhận ba bước UI vừa hướng dẫn đều trả về đúng: F5 vẫn hiển thị tài khoản, đăng xuất chuyển về đăng nhập, mở lại trang tài khoản sau đăng xuất chuyển về đăng nhập. Ghi nhận bằng chứng người dùng cho phiên đăng nhập trên giao diện VI; chưa tự thao tác trình duyệt, chưa có trình duyệt/kích thước hoặc kiểm tra EN. FE-04 vẫn mở do còn phạm vi giao diện khác. Đọc server/config.js, server/db.js và truy vấn trong server/app.js/server/index.js để chuẩn bị cấu hình tài khoản MySQL ứng dụng riêng; chưa tạo tài khoản hoặc đổi cấu hình kết nối. Chỉ cập nhật PROJECT_CHECKLIST.md. Tiếp theo: cấu hình tài khoản ứng dụng riêng và hướng dẫn chạy, kiểm tra EN/mobile, triển khai ticket khi được yêu cầu.
Ngày 2026-09-21 · BE-01, AUTH-01, FE-04: Người dùng cung cấp nội dung trang tài khoản tiếng Việt có thông báo xác thực thành công, thông tin tài khoản và vai trò Khách hàng. Đối chiếu account.js: thông báo thành công xuất hiện sau API auth/me và workspace theo vai trò trả thành công. Ghi nhận luồng hiển thị tài khoản đã hoạt động theo bằng chứng người dùng; không lưu họ tên/email vào checklist. Chưa có bằng chứng riêng về đăng ký mới, tải lại trang, thao tác đăng xuất, giao diện EN hoặc bố cục desktop/mobile. Chỉ sửa PROJECT_CHECKLIST.md, không sửa mã/database. Tiếp theo: kiểm tra tải lại và đăng xuất qua UI, cấu hình tài khoản MySQL riêng cho ứng dụng, triển khai ticket khi được yêu cầu.
Ngày 2026-09-21 · AUTH-01, BE-01, DB-02, DB-03, DB-06, QA-02: Người dùng cung cấp log RUN_MYSQL_TESTS=1 và npm.cmd test: tests 10, pass 10, fail 0, skipped 0, duration_ms 6036.632. Ghi nhận bằng chứng từ log người dùng, không khẳng định trợ lý tự chạy lại. Test bao gồm migration giữ dữ liệu cũ/chạy lại/an toàn init, liên kết tài khoản khách, email trùng, đầu vào và origin, mật khẩu/session, quyền khách/vai trò, file nhạy cảm, giới hạn thử, khóa tài khoản/hết phiên/đăng xuất, một số ràng buộc sở hữu/đánh giá. Đánh dấu AUTH-01 đạt phạm vi backend; giữ FE-04 và QA-02 mở do chưa kiểm tra trình duyệt và toàn bộ nghiệp vụ. DB-02/DB-03/DB-06 có bằng chứng một phần, chưa đánh dấu toàn bộ thiết kế hoàn thành. Chỉ cập nhật PROJECT_CHECKLIST.md; chưa sửa mã hay database. Tiếp theo: kiểm tra npm start trên qlkhviettin và giao diện VI/EN, cấu hình tài khoản MySQL ứng dụng riêng, triển khai luồng ticket.
Ngày 2026-09-21 · DB-02 đến DB-06, BE-01, AUTH-01, QA-02: Log người dùng chạy npm.cmd run db:check trả { status: 'current', steps: 15 }. Đối chiếu server/migrate.js: nhật ký có đủ 15 bước done và checksum phù hợp; chưa phải kiểm tra độc lập toàn bộ schema hay nghiệp vụ. Ghi nhận migration đã được công cụ xác nhận hiện hành, chưa đánh dấu các mục nghiệp vụ hoàn thành. Đọc tests/integration.test.js để hướng dẫn RUN_MYSQL_TESTS=1 và npm.cmd test: bộ test tạo database vietincare_test_<chuỗi ngẫu nhiên>, tự dọn database thử sau chạy, không dùng qlkhviettin làm database test. Chỉ sửa PROJECT_CHECKLIST.md; chưa có kết quả test tích hợp mới. Tiếp theo: chạy test MySQL, cấu hình tài khoản ứng dụng riêng, kiểm tra đăng ký/đăng nhập trên trình duyệt.
Ngày 2026-09-21 · BE-01, DB-02 đến DB-06: Log người dùng chạy npm.cmd run db:check trả { status: 'ready', remaining: 15 }. Đối chiếu server/migrate.js: kết nối thành công, kiểm tra phiên bản và dữ liệu trước migration đạt, còn 15 bước SQL chưa áp dụng. Đây không phải 15 bảng mới và không phải kết quả kiểm thử nghiệp vụ. Hướng dẫn chạy db:migrate trong cùng phiên PowerShell rồi db:check để xác nhận current. Chỉ sửa PROJECT_CHECKLIST.md, chưa nhận kết quả migration, không đánh dấu các mục triển khai hoàn thành và không lưu thông tin xác thực. Tiếp theo: áp dụng/kiểm tra migration, cấu hình tài khoản ứng dụng, kiểm thử backend/auth.
Ngày 2026-09-21 · DB-07, BE-01, AUTH-01: Log người dùng sau nạp SQLQuery1.sql hiển thị Query OK và SHOW TABLES FROM qlkhviettin trả đủ 15 bảng khởi tạo, đối chiếu đúng danh mục trong script. Đánh dấu DB-07 hoàn thành theo tiêu chí khởi tạo database cơ bản; không đánh dấu DB-02 đến DB-06, BE-01 hoặc AUTH-01. Đọc package.json, server/config.js, server/index.js và server/migrate.js để hướng dẫn cấu hình biến môi trường tạm trong PowerShell, nhập mật khẩu ẩn và chạy db:check trước migration. Chỉ sửa PROJECT_CHECKLIST.md; chưa chạy migration hay kiểm thử backend trong phiên này. Tiếp theo: db:check, db:migrate khi kiểm tra đạt, cấu hình tài khoản ứng dụng và kiểm thử xác thực.
Ngày 2026-09-21 · DB-07: Người dùng cung cấp kết quả SHOW TABLES FROM qlkhviettin là Empty set; xác nhận database hiện chưa có bảng theo phiên root đã đăng nhập. Đọc lại SQLQuery1.sql, xác nhận script khởi tạo có 15 CREATE TABLE và không có lệnh DROP/TRUNCATE/DELETE. Hướng dẫn nạp script vào database trống rồi kiểm tra danh sách bảng. Chỉ cập nhật PROJECT_CHECKLIST.md; chưa trực tiếp nạp SQL, chưa nhận kết quả khởi tạo, chưa đánh dấu hoàn thành. Tiếp theo: xác nhận đủ 15 bảng ban đầu, cấu hình kết nối backend và áp dụng migration qua server/migrate.js.
Ngày 2026-09-21 · DB-07, BE-01: Căn cứ log CLI do người dùng cung cấp, xác nhận đăng nhập root thành công trên 127.0.0.1:3306, SELECT VERSION() trả 8.0.46 và SHOW DATABASES có qlkhviettin. Lệnh đọc bảng lỗi 1064 vì có hai dòng FROM liên tiếp. Cập nhật trạng thái DB-07 từ bị chặn xác thực sang đang kiểm tra schema; hướng dẫn SHOW TABLES FROM qlkhviettin. Chỉ sửa PROJECT_CHECKLIST.md; chưa chạy SQL thay đổi dữ liệu, chưa xác nhận backend hoặc migration. Kiểm tra: đối chiếu trực tiếp log người dùng, không coi đây là kiểm thử tích hợp.
Ngày 2026-09-21 · Rà soát tiến độ DB-02 đến DB-07, BE-01, AUTH-01, DOC-05, QA-02, RELEASE-01: Đọc checklist, PROJECT_SCOPE.md, README, mã backend/frontend, migration và test hiện có. Xác nhận có trang tài khoản VI/EN, auth.js/account.js gọi API; backend Express có đăng ký/đăng nhập/đăng xuất, session và kiểm tra vai trò. Migration 001 đã có liên kết Customer–UserAccount, ràng buộc sở hữu/trạng thái/đánh giá, TicketReply, TicketStatusHistory, MessageSource, SystemSetting và mốc handover; đây là mã chưa được xác nhận áp dụng, không đánh dấu DB-02 đến DB-06 hoàn thành. MySQL80 Running; chạy npm run db:check báo .env không tồn tại và ER_ACCESS_DENIED_ERROR với cấu hình mặc định hiện tại. Chưa kiểm tra được schema thực; không thay đổi database. Kiểm tra cú pháp toàn bộ JS trong server/, tests/ và script.js/auth.js/account.js đạt. Chạy node --test tests/*.test.js: 10 test, 1 pass, 9 skip, 0 fail; chưa bật kiểm thử MySQL. Phát hiện có 05_ban1.docx (52.679 byte), chưa đọc nội dung/render nên chưa xác định có thay thế tài liệu kiểm thử 05 được nhắc trước đây hay không. README còn ghi backend chưa triển khai, chưa có hướng dẫn backend đầy đủ; cần đồng bộ tại RELEASE-01. Không sửa mã/SQL/Word, không xác nhận nghiệp vụ hay bố cục. Tiếp theo: xử lý DB-07 và kiểm thử BE-01/AUTH-01; hoàn thiện luồng TK-01 đến TK-03; đối chiếu tài liệu 05 và kiểm tra bố cục DOC-02.

Ngày 2026-09-21 · SCOPE-01, DOC-02 đến DOC-05, AUTH-02, EVAL-01, ADMIN-01 đến ADMIN-04: Theo yêu cầu đồng bộ tài liệu và chỉ dùng MySQL, sửa 3 Word 02/03/04, thêm `PROJECT_SCOPE.md`, sửa README và phân kỳ checklist. Lưu bản trước sửa tại `.mysql-sync/scope-before-2026-09-21.zip`; script hỗ trợ `.mysql-sync/sync-scope.ps1`, `.mysql-sync/refine-scope.ps1`. Thay 40 vị trí sơ đồ ảnh cũ bằng luồng/quan hệ chữ để loại nội dung cũ khỏi phần hiển thị; bản ảnh gốc còn trong backup. Kiểm tra XML và nội dung, xác nhận MySQL 8.0.46, phạm vi/nhãn chung và không còn tham chiếu hệ quản trị khác trong văn bản hiện hành. Không có bộ runtime/render tài liệu đi kèm trong môi trường này nên chưa render/xem PNG; không ghi Pass bố cục. Chưa sửa SQL/database/frontend, chưa chạy kiểm thử nghiệp vụ. Thiết kế đích có các bảng/liên kết cần migration được ghi rõ là chưa triển khai. Tiếp theo: DOC-02/DOC-04, DB-02 đến DB-06, BE-01/AUTH-01.

Ngày 2026-09-21 · DOC-03, DOC-04, DOC-05, SCOPE-01: Rà soát nội dung văn bản/bảng OOXML của 3 DOCX hiện có, README, SQL, JavaScript và cấu trúc frontend VI/EN. Phát hiện danh mục thiếu file kiểm thử 05; tài liệu 02 có phạm vi triển khai rộng và mốc 27/09/2026. Xác nhận các điểm cần thống nhất: UC001 chỉ nêu người dùng nội bộ nhưng thiết kế 04 có đăng nhập khách hàng; 03 tạo ticket khi yêu cầu phức tạp còn 04 tạo trước AI; trạng thái Mới/Chờ xử lý; cảm xúc 3/4 nhãn; phản hồi dưới 2 giây so với dưới 5 giây; UC015 có tên tài khoản nhưng nội dung cấu hình. SQL chưa liên kết Customer–UserAccount, chưa có nơi lưu phản hồi ticket độc lập với chat, tệp đính kèm UC003 hoặc cấu hình UC015. Chỉ cập nhật phát hiện/checklist, không sửa mã nguồn, SQL hoặc Word, không đánh dấu chức năng hoàn thành. Chưa render/xem sơ đồ ảnh Word, chưa chạy trình duyệt, backend hoặc MySQL trong phiên này; trạng thái DB-07 vẫn theo lịch sử, chưa xác minh lại.

Ngày 2026-09-20 · DB-07: Log người dùng cung cấp cho thấy lần chạy đặt lại mật khẩu bằng init-file thất bại do không tìm thấy `C:\mysql-init.txt` (OS errno 2); MySQL đã Shutdown complete. Chưa xác nhận đổi mật khẩu hoặc đăng nhập thành công. Tiếp theo: kiểm tra tên/vị trí file tạm ngoài project, chạy lại rồi kiểm tra đăng nhập. Không đọc hoặc lưu nội dung file chứa mật khẩu vào project.

| Ngày | Công việc | Thay đổi và kiểm tra | Phần còn lại |
|---|---|---|---|
| 2026-09-20 | FE-01, FE-02, FE-03 | Sửa frontend VI/EN; kiểm tra cú pháp JS và 72 kiểm tra bằng Node đạt trong phiên trước. | Kiểm thử trình duyệt FE-04; backend chưa có. |
| 2026-09-20 | DB-01, DOC-01 | Chuyển MySQL, tạo 15 bảng, kiểm tra dữ liệu mẫu đạt; cập nhật nội dung 3 Word và README, kiểm tra XML đạt trong phiên trước. | DOC-02 và chạy lại các test nghiệp vụ. |
| 2026-09-20 | TRACK-01 | Tạo `PROJECT_CHECKLIST.md`, `AGENTS.md`, liên kết README; đối chiếu danh sách file và README/SQL hiện tại để ghi trạng thái. | Bắt đầu DB-02, DB-03 khi người dùng yêu cầu triển khai tiếp. |
| 2026-09-20 | DB-07 | Kiểm tra dịch vụ MySQL80 đang dừng; thử khởi động trong và ngoài sandbox đều bị từ chối quyền. Chưa kết nối được database thực. | Khởi động MySQL80 bằng quyền Administrator, sau đó kiểm tra tài khoản kết nối và schema hiện có. |
| 2026-09-20 | DB-07 | Người dùng khởi động dịch vụ thành công; xác nhận Running. Thử kết nối root không mật khẩu trả lỗi 1045. | Cấu hình đăng nhập cục bộ rồi kiểm tra schema trước khi tạo bảng. |
| 2026-09-21 | BE-01, AUTH-01 | Đối chiếu workspace thấy đã có `server/` với Express, config/migration MySQL, API auth/session/phân quyền và test tích hợp. `node --check` các file JS đạt; `node --test tests/*.test.js` đạt 1 test mật khẩu nhưng skip 9 test MySQL vì chưa bật `RUN_MYSQL_TESTS=1`. | Cấu hình kết nối MySQL cục bộ, chạy test tích hợp thực tế; sau đó mới đánh giá Pass cho BE-01/AUTH-01. Ticket, chat lưu lịch sử, handover, RAG và dashboard vẫn chưa có API nghiệp vụ. |

## Lưu ý khi tiếp tục

- Không tự chạy lại `SQLQuery1.sql` trên database thật đã có bảng; `.mysql-sync/smoke.sql` chỉ dùng trong môi trường kiểm thử thích hợp.
- Bản sao Word trước chuyển MySQL từng được tạo tại `.mysql-sync/documents-before-mysql.zip`; kiểm tra file còn tồn tại trước khi cần khôi phục.
- Đã chọn Node.js/Express; xác thực backend đã qua test tích hợp MySQL theo log người dùng ngày 2026-09-21. Chưa kiểm tra giao diện trình duyệt. Danh sách việc mở không phải xác nhận các tính năng đã hoàn thành.
- Trợ lý nhắc tiến độ trong phiên làm việc với project; không có tác vụ chạy nền theo dõi thay đổi bên ngoài phiên.

Ngày 2026-09-22 · FE-07 · Dọn môi trường kiểm thử: đã đóng tab preview, reset viewport, dừng Node preview và xác minh @@port/@@datadir trước khi dừng MySQL tạm 33316; xóa database và thư mục ui-20260922 do đợt này tạo. Giữ nguyên .mysql-test/data cũ và dịch vụ thật 3306/3000. Tiếp theo: người dùng restart backend, Ctrl+F5 trên localhost:3000 để tải giao diện mới; CHAT-01/RAG là ưu tiên nghiệp vụ kế tiếp, chưa triển khai trong đợt giao diện.

Ngày 2026-09-22 · CHAT-01 · Dọn môi trường: đã đóng tab kiểm tra cuối, reset viewport, dừng Node preview, xác minh @@port/@@datadir trước khi dừng MySQL tạm 33316, xóa database preview và thư mục chat-20260922 do tác vụ tạo. Giữ nguyên .mysql-test/data cũ và database thật 3306. Ưu tiên tiếp theo: áp dụng migration 003 và kiểm tra bản thật; triển khai AI-01/RAG với nguồn tri thức xuất bản; sau đó HAND-01 chuyển giao cho Agent.


Ngày 2026-09-23 · AI-01, ADMIN-02, CHAT-01, FE-04, QA-02, RELEASE-01 · Chốt đợt OpenAI/RAG đã triển khai và kiểm thử ngày 2026-09-22:
- Người dùng chọn OpenAI và nhập khóa cục bộ khi chạy. Backend dùng Embeddings/Responses, lưu vector trong MySQL, chỉ truy xuất nguồn đã xuất bản; kiểm tra quyền/sở hữu, khóa chống sinh trùng và lưu nguồn lịch sử. Tài liệu đã xuất bản bất biến; bản thay thế xuất bản nguyên tử. Tin khách được giữ khi AI lỗi, có thử lại; không cung cấp khóa cho trình duyệt.
- File backend: server/openai.js, server/knowledge.js, server/rag.js, server/config.js, server/app.js, server/chats.js, server/index.js, server/migrate.js, server/provision-admin.js. Migration mới migrations/004_knowledge_rag.sql thêm 3 bước, tổng 22; giữ migration cũ. Scripts manage-local.ps1 thêm create-admin; start-local.ps1 thêm -EnableAI và hỏi khóa ẩn; .env.example chỉ placeholder.
- File giao diện: VI/knowledge-vi.html, EN/knowledge-en.html, knowledge.js/css, chat.js/css, workspace.js, account.js. Đồng bộ VI/EN, chờ AI, thử lại và nguồn mở rộng. Tài liệu: RAG_GUIDE.md, README.md, CHAT_GUIDE.md, PROJECT_SCOPE.md, migrations/README.md và checklist này.
- Kiểm thử thực thi: RUN_MYSQL_TESTS=1 npm.cmd test trên MySQL 8.0.46 tạm cổng 33316, 37 tests / 37 pass / 0 fail / 0 skip (~7,21 giây). tests/openai.test.js dùng transport giả; tests/rag.integration.test.js dùng provider giả và tài khoản runtime chỉ CRUD. Kiểm tra quyền, phiên bản, chống trùng, lỗi provider, thiếu nguồn, tranh chấp, ngừng xuất bản khi đang sinh, bản chụp nguồn và phiên kết thúc. Test chat/ticket cập nhật số bước 22. Cú pháp JS và parser hai script PowerShell đạt.
- Trình duyệt với MySQL thật/provider giả: Admin VI lưu/index/thử/xuất bản; EN và mobile 390×844; Customer EN gửi câu hỏi, đọc nguồn, F5 và chuyển VI giữ tin/nguồn. Ảnh desktop 1265×714 và mobile đã xem. Đây là kiểm tra giao diện/luồng, không phải chất lượng câu trả lời OpenAI.
- Chưa kiểm chứng: API OpenAI thật, quyền/quota/model của khóa người dùng, chất lượng/ngưỡng tìm kiếm, độ trễ/chi phí. Chưa migrate database thật 3306. Chưa có bộ nhớ đa lượt, tải PDF/DOCX, chuyển giao, CSAT hoặc báo cáo.
- Dọn môi trường ngày 2026-09-23: xác nhận không có listener cổng 33316/3001, xóa đúng .mysql-test/rag-20260922 và preview-rag.mjs do tác vụ tạo; giữ .mysql-test/data cũ. Không dừng PID cũ đã tái sử dụng; không sửa database thật 3306.
- Ưu tiên: áp dụng migration 004 và chạy -EnableAI; kiểm tra bộ câu hỏi/nguồn với API thật; sau đó triển khai HAND-01 trong phạm vi được giao.


Ngày 2026-09-23 · AI-05, AI-01, FE-04, QA-02, RELEASE-01 · Tích hợp Google Gemini theo yêu cầu:
- Thêm server/gemini.js và server/ai.js, cập nhật server/config.js/app.js: chọn AI_PROVIDER, GEMINI_API_KEY/GEMINI_CHAT_MODEL, mặc định launcher Gemini; giữ OpenAI tùy chọn rõ ràng, không tự chuyển khi lỗi. Gemini dùng generateContent JSON có nguồn và batchEmbedContents gemini-embedding-001 768 chiều, phân biệt RETRIEVAL_DOCUMENT/RETRIEVAL_QUERY, chuẩn hóa vector. Header x-goog-api-key, URL cố định, timeout, chặn redirect, lỗi không chứa chi tiết bí mật; từ chối output lỗi/bị chặn/cắt ngắn/nguồn không hợp lệ.
- server/knowledge.js, server/rag.js truyền mục đích query; lọc model/số chiều hiện có ngăn dùng lẫn vector cũ. server/chats.js/knowledge.js trả tên provider; chat.js và knowledge.js đồng bộ nhãn VI/EN. Tài liệu xuất bản OpenAI cần tạo phiên bản mới/index Gemini/xuất bản; giữ snapshot nguồn lịch sử. Không sửa SQL/migration, vẫn tổng 22 bước.
- scripts/start-local.ps1 thêm -AIProvider gemini|openai; nhập khóa ẩn, xóa khóa provider không chọn khỏi môi trường chạy rồi khôi phục khi kết thúc. .env.example chỉ placeholder. RAG_GUIDE.md, CHAT_GUIDE.md và README.md cập nhật hướng dẫn hai ngôn ngữ, chuyển model và giới hạn.
- Kiểm tra: node --test tests/gemini.test.js tests/openai.test.js đạt 8/8; toàn bộ RUN_MYSQL_TESTS=1 npm.cmd test trên MySQL 8.0.46 tạm 33316 đạt 43/43, 0 fail, 0 skip (~8,75 giây). tests/gemini.test.js kiểm tra request/khóa/vector/lỗi/nguồn; tests/rag.integration.test.js bổ sung dùng adapter Gemini thật với HTTP transport giả xuyên suốt index/preview/publish/chat/MySQL, đúng mục đích embedding, nhãn provider và loại vector cũ. Cú pháp JS thay đổi và parser PowerShell đạt.
- Chưa gọi Google thật, chưa kiểm tra quota/model/key hoặc chất lượng/ngưỡng/độ trễ thực. Không kiểm tra lại giao diện bằng trình duyệt ở đợt đổi nhãn này; không dùng bằng chứng UI đợt trước thay thế. AI-05/AI-01 giữ mở đến kiểm chứng thật.
- Đã xác minh @@port/@@datadir trước SHUTDOWN MySQL tạm, xác nhận cổng không còn nghe rồi xóa đúng .mysql-test/gemini-20260923. Giữ .mysql-test/data cũ và database thật 3306; không lưu khóa thật.
- Tiếp theo: khởi động -EnableAI -AIProvider gemini, nhập khóa cục bộ; tạo lại dữ liệu tìm kiếm/xuất bản bằng Gemini rồi thử câu hỏi có nguồn và thiếu nguồn; sau đó HAND-01 khi tiếp tục nghiệp vụ.
